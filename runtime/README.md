# Bygdok Eternal Runtime

This directory is reserved for the future Android-native AzerothCore runtime package. Runtime packaging will eventually contain:

- `authserver`
- `worldserver`
- required Android ARM64 shared libraries
- configuration templates
- runtime metadata

No binaries, Termux libraries, runtime downloader, or executable launcher are included yet.